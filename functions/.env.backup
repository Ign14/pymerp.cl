# Firebase Cloud Functions - Environment Variables
# Instrucciones:
# 1. Copiar este archivo a .env en el mismo directorio
# 2. Reemplazar los valores de ejemplo con los valores reales
# 3. NUNCA subir el archivo .env a git (ya está en .gitignore)

# SendGrid API Key para envío de emails
# Obtener desde: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.REDACTED

# Email del administrador para notificaciones
ADMIN_EMAIL=ignacio@datakomerz.com

# Dominio de la empresa (para referencia)
COMPANY_DOMAIN=pymerp.cl
